{
  "name": "mercadopago-webhook",
  "version": "1.0.0",
  "description": "Webhook receptor de notificaciones de MercadoPago",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "mongodb": "^6.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.56.0"
  }
}
